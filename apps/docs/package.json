{
  "name": "docs",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vp dev --port=3005",
    "deploy": "wrangler deploy",
    "cf:typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "catalog:",
    "wrangler": "catalog:"
  }
}
